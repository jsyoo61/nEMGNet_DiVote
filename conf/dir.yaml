
dir:
  project: C:\Users\jsyoo\Mirror\BMI Lab\Research\EMG\code_publish
  data_raw: ${dir.project}/data/raw


  data_processed: ${dir.project}/data/processed # Where the signal segments are
  data_cohort: ${dir.data_processed}/cohort.csv # Where the signal segment summary is
